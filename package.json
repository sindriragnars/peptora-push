{
	"name": "peptora-push",
	"version": "0.2.0",
	"private": true,
	"type": "module",
	"description": "Web Push backend for Peptora WebApp (app.peptora.app). VAPID-signed push delivery for dose reminders + news article notifications. Runs as a node container on Coolify (push.peptora.app).",
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"build": "tsc",
		"start": "node dist/server.js"
	},
	"dependencies": {
		"ioredis": "^5.4.1",
		"web-push": "^3.6.7"
	},
	"devDependencies": {
		"@types/node": "^22.10.2",
		"@types/web-push": "^3.6.4",
		"@vercel/node": "^5.0.0",
		"typescript": "^5.7.2"
	}
}
